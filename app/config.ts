// API_URL Supprimée car pointait vers le serveur Node.js obsolète
// export const API_URL = 'http://51.75.200.205:3000/api';
// STREAM_URL Supprimée car obsolète

const config = {
  // API_URL, // Supprimée de l'export
  // STREAM_URL Supprimée de l'export
};

export default config;
